import { TableColTotalsPipe } from './table-col-totals.pipe';

describe('TableColTotalsPipe', () => {
  it('create an instance', () => {
    const pipe = new TableColTotalsPipe();
    expect(pipe).toBeTruthy();
  });
});
