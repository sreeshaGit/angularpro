import { TitleFormationPipe } from './title-formation.pipe';

describe('TitleFormationPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleFormationPipe();
    expect(pipe).toBeTruthy();
  });
});
